# --- Donnees d'environnement reelles : NE JAMAIS committer ---
# (copie ta version depuis les modeles .example)
/config.psd1
/ip-ranges.csv
decommissioning.json
decom-config.psd1

# --- Lanceurs .bat internes (contiennent le chemin UNC du share) : NE JAMAIS committer ---
# (ex : PCPulse-Dashboard.bat, Decommission-PC.bat)
*.bat

# --- Sorties generees par le Dashboard ---
PCPulse-Dashboard-*.html

# --- Backups d'ACL crees par Setup-Server.ps1 ---
.acl-backup/

# --- Cruft OS ---
Thumbs.db
.DS_Store
